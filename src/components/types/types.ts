export type assetType = {
    total_supply: number,
    value: number,
    name: {
        label: string,
        type: string,
        url: string
    }
}

export type assetPieType = {
    label: string;
    amount: number;
}